# codeagent-minimal 可提交集去依赖 + 独立成库检查报告

- 日期：2026-08-23
- 目标：可提交集（13 原子 + 支撑）是否**纯标准库（无外部第三方依赖）** + **可独立成库（可独立部署、无三体/内部路径/内部依赖断链）**，找问题并修复。
- 验证方式：全部真实验证（AST import 扫描 + 干净环境拷贝 + 真实加载 + 真实 pytest），非读文档臆断。
- 结论：**可提交集已满足「纯标准库 + 独立成库」**；发现并修复 1 类真实断链（测试硬编码旧原子数导致提交树非全绿）。

---

## 0. 可提交集范围界定（重要：与任务清单的一处差异）

任务清单列出的 13 原子：`process-sandbox / command-approvals / approval-orchestrator / context-compact / model-fallback / guard / minimalist-style / atomicity-audit / ontology-review / localized / domain-review / arch-review / bug-deep`。

实际已提交（018de01）的 13 个新开源原子为：`command-approvals / process-sandbox / security-scan / guard / arch-review / atomicity-audit / bug-deep / context-compact / domain-review / localized / model-fallback / ontology-review / minimalist-style`。

> 差异说明：`approval-orchestrator` 在提交 018de01 中被有意移入 `_pending_atoms/`（与 secret-vault/session/threebody-bridge 一并列入「不提交原子」），**不在 registry 也不在已提交集合内**。其代码本身纯 stdlib、自洽（import atomic_base/approval_policy/bug_deep，均已在提交集），如需纳入独立库需单独提交并补 registry 条目（见 §4 遗留）。因此独立部署验证以**已提交的 13 原子 + 支撑**为准。

支撑：`approval_policy.py / arch_review.py / known_defects.py / atomic_base.py / agent_loader.py / registry.json` 及其传递依赖支撑库（`pathguard / bug_deep / security_scan / chain_break` 等，均为本仓库内模块）。

---

## 1. 去依赖检查（纯标准库 / 无外部第三方依赖）

### 方法
对全部 `.py` 用 `ast` 解析所有 `import/from`，对照 `sys.stdlib_module_names` 判定；区分「模块级 import」与「函数内可选 import」。

### 结果

**13 原子入口文件 + 支撑文件：模块级 import 100% 标准库**，只额外 import 本仓库内部模块（同仓自洽，非外部依赖）：

| 文件 | 第三方 import | 判定 |
|---|---|---|
| 13 原子 `main.py` | 无（仅 `os/sys/json/ast/re/subprocess/tempfile/shutil/importlib` + 仓内 `atomic_base/approval_policy/arch_review/bug_deep/pathguard/security_scan/chain_break/agent_loader`） | ✅ 纯标准库 |
| `approval_policy.py` | `fnmatch/os/re` | ✅ 纯标准库 |
| `arch_review.py` | `ast/os/re/pathlib/collections` | ✅ 纯标准库 |
| `known_defects.py` | `re/json/sys` | ✅ 纯标准库 |
| `atomic_base.py` | `traceback/datetime/argparse/json/sys` | ✅ 纯标准库 |
| `pathguard / bug_deep / security_scan / chain_break / agent_loader` | 均为标准库 + 仓内模块 | ✅ 纯标准库 |

**全仓库（含非提交集）唯一命中的外部第三方 import，全部为「可选/函数内/非提交集」**：

| 文件 | 第三方 | 性质 |
|---|---|---|
| `dep_scan.py` | `stdlib_list` | **可选**：`try/except` 内，失败退化为内置标准库名集合 |
| `review.py` | `bandit` / `pyflakes` | **可选**：函数内、guard 后引用，缺失时静默降级 |
| `test_harness.py` | `pytest` / `coverage` | 测试工具，可选 |
| `atoms/*.py` | `numpy` / `sklearn` | 数据科学原子，不在 13 原子提交集 |
| `examples/*` | `crewai` / `langchain` | 演示示例，不在提交集 |
| `web/_cdp_verify.py` | `websocket` | 浏览器验证辅助，不在提交集 |

### 判定
✅ **可提交集（13 原子 + 支撑）纯标准库、零强制第三方依赖**，模块级加载不依赖任何外部包；`stdlib_list/bandit/pyflakes/pytest/coverage` 均不影响提交集运行（可选降级路径）。

---

## 2. 独立成库检查（无三体引用 / 无内部路径 / 无内部依赖断链）

### 2.1 三体 / 绝对路径 / 硬编码
- 提交集内 **无 `threebody/三体` 引用**（`git grep` 零命中）。
- 无 `E:/`/`C:/`/`/home/` 等**硬编码绝对路径**。仅有的 `E:/...` 命中全部为：测试夹具（故意测路径穿越拒绝）、`minimalist-style` 的 `_ABS_PREFIX` 检测列表（功能，非依赖）、文档示例、`web/_cdp_verify.py` Chrome 路径（非提交集）。均非依赖。
- 13 原子入口均用**可移植相对路径**注入 repo root（`HERE=os.path.dirname(os.path.abspath(__file__)); REPO_ROOT=dirname(dirname(dirname(HERE))); sys.path.insert(0,REPO_ROOT)`），随文件位置迁移，不绑定本机路径。

### 2.2 registry / import 断链
- registry.json 重建为 **29 原子**；`load_registry()` 依赖拓扑解析：**conflicts=[]（0 冲突），order 全解析**。
- 无「依赖未提交/闭源能力」断链：`depends_on` 全部指向 registry 内 `provides` 索引。唯一 `model-fallback` 声明 `depends_on=['llm.list_models']`，`llm-router` 在 registry 内提供 `llm.list_models`，解析正常（且 model-fallback 实际不 import llm-router，仅元数据声明，运行时无硬耦合）。
- 13 原子仅依赖仓内模块（均在本提交集传递闭包内），无跨未提交原子引用。

### 2.3 判定
✅ **独立成库**：无三体引用、无绝对路径、无内部库依赖未提交件，registry 加载无断链。可整体拷贝独立部署。

---

## 3. 独立部署验证（干净环境实测）

### 方法
`git archive HEAD | tar -x` 导出**已提交树**（无 `.git`、无 `_pending_atoms`、无 `__pycache__`、无未跟踪产物）到全新临时目录 `/tmp/clean_verify2`，不依赖用户系统任何状态，直接运行。

### 3.1 registry 加载
```
python agent_loader.py --load-all
→ 29 原子全部 [ready]，degraded=[], conflicts=[]
```
13 个已提交目标原子逐个确认 status=ready：process-sandbox / command-approvals / context-compact / model-fallback / guard / minimalist-style / atomicity-audit / ontology-review / localized / domain-review / arch-review / bug-deep / security-scan。

### 3.2 测试全绿
```
python -m pytest tests/ -q
→ 146 passed in 95.14s
```
（修复后全绿；修复前见 §4。）

### 3.3 判定
✅ **已提交集单独拷贝到干净环境即可独立运行**：registry 全 ready、测试全绿，不依赖用户系统路径/三体/闭源 orchestrator/assembler/codemode（相关闭源测试经 `if not exists: return` 守卫自动跳过）。

---

## 4. 发现的问题与修复

### 🔴 问题（真实断链，已修复）
**提交树非全绿**：018de01 新增 13 原子（registry 16→29）时，未同步更新 2 个融合测试仍硬编码「16 原子」，导致干净环境实测 **3 failed / 143 passed**，与提交信息声称的「143 passed」不符。

| 文件 | 断链 | 修复 |
|---|---|---|
| `tests/test_codeagent_atomic_p1.py` | `EXPECTED_ATOMS` 旧 16 原子；`test_sixteen_atoms_load_ready` 断言 `len==16` | 对齐 registry 29 原子；改名 `test_all_atoms_load_ready`；`len==len(EXPECTED_ATOMS)` |
| `tests/test_codeagent_fusion.py` | `FUSED_ATOMS` 旧 16 原子；`test_runtime_16_atoms_ready_no_degraded` 断言 `count==16`；`test_unified_entry_api_atoms` 断言 `count==16` | `FUSED_ATOMS` 对齐 29 原子；改名 `test_runtime_all_atoms_ready_no_degraded`；`count==len(FUSED_ATOMS)` |

修复后重新干净环境实测：**146 passed, 0 failed**（新增 3 个通过 = 原 3 个失败用例修复）。

### 🟡 遗留/说明（不阻塞，如实记录）
1. `approval-orchestrator` 在 `_pending_atoms/`，**未被提交**、不在 registry。若需纳入独立库：提交其目录 + 补 registry 条目（代码本身纯 stdlib、自洽，依赖均已在提交集）。当前作为「待定原子」记录。
2. `tests/test_codeagent_atomic_p1.py:145` 闭源 `assembler.detect_conflicts` 仍断言 `len==16`：该测试经 `CLOSED_DIR` 守卫，在开源提交集内恒跳过，不影响全绿；闭源侧若升级需同步。
3. 可选第三方（`stdlib_list/bandit/pyflakes/pytest/coverage`）属全仓库非提交集件或可选降级，不破坏「纯标准库」结论。

---

## 5. 最终结论

✅ **可提交集（13 原子 + 支撑）已完成去依赖 + 独立成库：**
- **纯标准库**：模块级 import 零外部第三方，可选依赖均有降级路径。
- **独立成库**：无三体引用、无绝对路径硬编码、registry 0 冲突、无未提交原子断链。
- **独立部署验证**：干净环境拷贝 `registry 29 原子全 ready + pytest 146 passed 全绿`。
- **真实修复**：2 个测试硬编码旧 16 原子数的断链已对齐 registry 29 原子，提交树从 3-failed 修至全绿。

验证与修复均基于真实执行（AST 扫描 / `git archive` 干净拷贝 / `agent_loader --load-all` / `pytest`），无臆断。

# ── Python ────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── 本地运行产物 / 数据（数据不出厂：不提交本地库 / 任务状态 / 经验记忆 / 报告 / 模型密钥）──
lab/lab_data/
lab/**/lab_data/
*.db
*.sqlite
.taskstate/
.codeagent/
.codeagent_snapshots/
experience/
*.log
# 原子运行时数据（事件流落盘 / 修复备份：属本地运行产物，不入库）
_store/
*_repair_backup_*/
.runtime.jsonl

# 测试截图 / 临时图
*.png

# 环境
.venv/
venv/
lab/lab_config.json

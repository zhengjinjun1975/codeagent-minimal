# Contributing

欢迎提 issue 和 PR。这是给需要审代码但不想写代码的人用的代码审查智能体，纯标准库零依赖。

## Bug 报告
- 附一个能复现的代码片段 + 期望的审查结论 / 实际输出
- 标注环境（Python 版本 / 操作系统 / 是否用 LLM 审查）

## PR 要求
- 代码 + 对应测试；跑通 `python -m pytest test_review.py -v`
- 保持纯标准库零依赖：新增功能优先用 Python 标准库，不引第三方
- 审查规则改动附样例输入输出（保证不破坏既有审查项）
- 示例代码一律用公开/虚构片段，不夹带真实项目代码

## 提交规范
- 提交信息：`feat:` / `fix:` / `chore:` / `docs:` 前缀 + 简述
- 版本号递增：改 `__version__` / README 徽标 / CHANGELOG 保持一致
- 注释用中文，面向"要审代码但不写代码"的人
